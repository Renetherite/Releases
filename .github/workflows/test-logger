name: Test Scheduled Workflow

on:
  schedule:
    - cron: '*/1 * * * *'  # every 1 minute
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Log time
        run: |
          echo "Scheduled workflow ran at $(date)"
